micronaut:
  application:
    name: heuteabstimmung-app

datasources:
  default:
    driverClassName: org.postgresql.Driver
    username: 'demo'
    password: 'x114d24x9123qht4gps1'

jpa:
  default:
    packages-to-scan:
      - 'com.noser.heuteabstimmung.core.ports.persistence'
    properties:
      hibernate:
        hbm2ddl:
          auto: none
        show_sql: true
# https://micronaut-projects.github.io/micronaut-flyway/latest/guide/index.html#additionalConfig
flyway:
  datasources:
    default:
      enabled: true